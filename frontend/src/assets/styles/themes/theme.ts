/**
 * Objeto que define o tema da aplicação, incluindo cores e fontes.
 */
const theme = {
  /**
   * Cores do tema.
   */
  colors: {
    /** Cor primária. */
    primary: '#007bff',
    /** Cor secundária. */
    secondary: '#0056b3',
    /** Cor de fundo. */
    background: '#f5f5f5',
    /** Cor do texto. */
    text: '#333',
  },

  /**
   * Fontes do tema.
   */
  fonts: {
    /** Fonte do corpo do texto. */
    body: 'Arial, sans-serif',
    /** Fonte dos títulos. */
    heading: 'Georgia, serif',
  },
};

export default theme;