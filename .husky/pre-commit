<<<<<<< HEAD
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running lint-staged..."
npx lint-staged || {
  echo "❌ Lint-staged failed. Fix the errors before committing."
  exit 1
}
echo "🔍 Running ESLint..."
npx eslint --fix . || {
  echo "❌ ESLint failed. Fix the errors before committing."
  exit 1
}
echo "✅ Pre-commit checks passed. Proceeding with the commit."
=======
. "$(dirname "$0")/_/husky.sh"
npx lint-staged
npm test
>>>>>>> c5eaf2d2f34e4ab998d3b5749b22a754c50f7ee0
npx lint-staged
npx lint-staged
